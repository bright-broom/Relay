# Relay 開発要件

ユーザー指定の開発方針。初期画面サンプルより本書を優先する。

## ビジョン

特定の会社・住宅業界に限定せず、幅広い営業と営業以外の職種でも使える顧客・案件・活動の基盤を目指す。入力負荷を最小限にし、会話や活動から次の対応を提案する。AIによる推測と確定済みの事実は区別する。

## 必須要件

- 本番コードはTypeScriptを必須とし、strictな型検査を有効にする。シミュレーション・計算にも型を付ける。
- 実装開始時に言語・フレームワーク・ランタイム・ライブラリの最新安定版と相互互換性を公式情報で確認する。採用版はlockfileで固定する。
- モダンなUI/UXライブラリを積極的に活用し、アクセシビリティ、操作の一貫性、表示速度を確保する。
- 色・余白・文字サイズ・角丸等のハードコードを避け、デザイントークンとテーマで中央管理する。共通コンポーネントを使う。
- 表示テキストはグローバルな翻訳リソースへ集約する。コンポーネントに表示文言を直書きしない。
- 多言語追加を前提とし、日付・金額・数値・複数形・タイムゾーン・RTL・言語フォールバックを設計する。「全言語対応」は全言語の翻訳済みを意味せず、言語を拡張可能な構造を必須とする。
- 計算はUIから分離した型付きモジュールとする。用途に合う計算ライブラリを調査して活用し、金額の精度・丸め・単位・再現性を明示する。重要な計算は境界値を含むテストを行う。
- Vercelへのホスティングを前提とする。
- リポジトリは初期段階ではpublicで運用する。公開してよいコード・資料・架空データのみを保存する。
- CI/CDは変更対象を絞り、同じブランチの古い実行をキャンセルし、キャッシュを活用する。ドキュメントのみの変更で重いビルドを走らせない。

## 初期サンプルの位置付け

`prototype/index.html` はこれらの要件が確定する前の画面検討用HTML。インラインCSS・日本語直書き・JavaScriptを含むため、本番実装のコード雛形として流用しない。画面の流れを参考にし、TypeScript・デザインシステム・国際化に沿って作り直す。

## 現時点で未確定

UI・国際化・計算ライブラリの具体名と版、初期提供言語、課金プラン、外部連携の優先順。実装時に要件と互換性を照合して確定する。
