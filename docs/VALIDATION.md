# 2026-09-13 更新

「デザインと構成」ページ・ナビゲーション・専用スタイル・表示文言を削除。旧 `#system` は今日の対応へフォールバックする。開発用のデザインガイドとトークンは引き続き使用する。以下は過去の検証記録。

# デザイン刷新の確認状況

2026-09-11

## 今回の見直し

- クリーンアップ：未使用翻訳キー61件、CSSセレクター39件、トークン10件、到達しない処理・未使用アイコン・受信側のないイベントを除去。詳細は `CODE_REVIEW.md`。
- 保存対象が変わらない操作の書き込み省略、別ウィンドウの先行保存の検知、不正な保存値の拒否を追加・テスト。保存失敗・競合は成功表示を取り下げ、画面へ残す。
- `npm run audit` で文言・CSSの保守的な参照監査を追加し、`npm run check` に組み込んだ。依存関係の `npm audit` でも既知の脆弱性0件。
- レスポンシブ：今日の対応、案件一覧・詳細、更新案、取込、デザインの全6画面と共通ダイアログへ適用。案件一覧はPCの表／タブレットの2列カード／スマートフォンの1列カードへ切り替える。
- 480 / 768 / 1024pxの幅区分、狭い高さ、長文折り返し、フィルターの縦配置、安全領域、可視領域に合わせたダイアログを実装。検索や条件変更は表とカードの両方を更新する。
- 想定確認幅は320 / 390 / 480 / 768 / 1024 / 1440 / 1920px。これらは実装の対象幅であり、実ブラウザーで測定済みの結果ではない。ブラウザーの管理者ポリシー拒否が未解消のため、はみ出し量の実測・スクリーンショット比較・実機操作は未実施。
- iOS / Androidのホーム画面向けPWA、manifest、180/192/512pxアイコン、オフライン用のアプリ本体キャッシュ、端末内保存、安全領域への対応を追加。
- 純粋なテストで保存・復元、破損データ・容量不足、PNGサイズ、manifest、アプリ本体だけをキャッシュすること、オフライン時のトップ画面応答、古い自アプリキャッシュの削除を検証。
- iPhone / Android実機でのインストール、起動、キーボード表示、安全領域、オフライン起動は未検証。単体テストは実機検証の代わりではない。
- 全画面の重複説明を削減。ナビゲーションと移動・コピー等をアイコン化。デスクトップは細いナビゲーション、狭幅では開閉式メニュー。
- 一覧の担当・表示条件・状態をプルダウン化。詳細の背景・履歴・根拠は折りたたみ、デザイン仕様も選択式。
- 対応記録を連絡方法・結果・保存方法の選択式へ変更。定型結果なら入力文字数0で保存可能。「その他」だけ補足必須。案件間の移動と言語変更でも下書きを保持する実装。
- 純粋な記録処理のテスト：未選択の拒否、定型結果の文字入力なし保存、了承・無応答・返答待ちで完了しないこと、明示的な完了、二重完了の拒否、予定・待機先を勝手に変更しないこと。
- 色を墨黒・白・青の3つの基礎値に限定。灰色と操作時の濃淡を生成式で管理。状態の赤・黄・緑を撤去。
- 本文16px、セクション24px、ページ32/28px、行高と日本語字間の役割を修正。判断根拠・フォーム補足を本文サイズへ戻した。
- 補助的な黒いカードを区切り線の領域へ変更。主要操作に青を集中。期限超過を文字でも明示。
- 実際のトークンを使う色・文字・余白・状態の見本を「デザインと構成」へ追加。

## 実装した範囲

- 今日の対応、案件一覧、案件詳細、更新案確認、取込サンプル、デザインと構成の6画面。
- 根拠と引き継ぎのダイアログ、対応メモ、タスク完了、承認・却下、競合表示、検索結果0件、入力エラー。
- 中央トークンからCSS生成。UI文言は日本語・英語を中央管理し、言語切り替えを実装。
- 架空の案件データは元の日本語で表示。全言語翻訳済みではない。

## 実行済み

Vercelの本番公開完了。公開URLのHTML、manifest、Service Worker、JavaScript、3サイズのPNGがHTTP 200で配信されること、manifestのContent-Typeと再検証設定を確認した。公開URLを開くブラウザー操作は管理者ポリシーの確認ができず拒否された。実画面・実機の検証は未実施。

`npm run check`：TypeScript strict型検査、ビルド、日英キーと変数の一致、CSSのトークン参照、UIのインラインスタイル・日本語直書きの検査、3色の基礎定義と生成ルール、本文・見出しの意味トークン、本文および入力境界・フォーカスの単色コントラスト、ビルド成果物の存在確認。

## 未検証

前回はブラウザーの自動安全確認で管理者ポリシーを検証できず、localhostへのアクセスが拒否された。今回も開いている案件詳細タブの選択が Browser Use URL policy により拒否された。迂回していない。そのため390/768/1440pxの実際の見た目、クリック、キーボード、フォーカス復帰、200%拡大、RTL、読み上げ、実ブラウザーの状態遷移は未検証。ソースへの実装と実機での検証を区別する。

認証・DB・LLM・外部送信・ファイル解析は未接続。サンプルの状態は端末内のlocalStorageへ保存する。端末間の同期はなく、ブラウザーデータの削除や端末のストレージ整理で消失する場合がある。本番データの永続化にはDB接続が必要。
