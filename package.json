{
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8",
    "@modelcontextprotocol/client": "2.0.0",
    "@types/node": "22.20.2",
    "esbuild": "0.28.2",
    "typescript": "7.0.2"
  },
  "name": "relay-design-preview",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.mjs",
    "check": "npm run typecheck && npm run build && node scripts/check.mjs && npm run audit && npm run test:server && npm run test:calendar",
    "audit": "node scripts/audit.mjs",
    "test:server": "node tests/server.mjs",
    "db:migrate": "node scripts/migrate.mjs",
    "test:calendar": "node tests/calendar.mjs"
  },
  "dependencies": {
    "@js-temporal/polyfill": "0.5.1",
    "@modelcontextprotocol/server": "2.0.0",
    "openid-client": "6.8.8",
    "postgres": "3.4.9",
    "zod": "4.6.2"
  },
  "engines": {
    "node": "24.x"
  }
}
